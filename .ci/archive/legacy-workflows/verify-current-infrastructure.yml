name: Verify current Lean infrastructure

"on":
  push:
    branches:
      - gpt-pro/full-lean-certificate
    paths:
      - '.github/workflows/verify-current-infrastructure.yml'

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: gpt-pro/full-lean-certificate
      - uses: leanprover/lean-action@v1
        with:
          build: false
          test: false
          lint: false
          axiom-audit: false
          use-mathlib-cache: true
      - name: Verify every completed module
        shell: bash
        run: |
          test -f Sqrt6KissingBound/AnalysisChord.lean
          test -f Sqrt6KissingBound/AnalysisEstimate.lean
          test -f Sqrt6KissingBound/AnalysisCapData.lean
          test -f Sqrt6KissingBound/SphericalCaps.lean
          test -f Sqrt6KissingBound/CapPacking.lean
          test -f Sqrt6KissingBound/CapCancellation.lean
          lake build
          pattern='(^|[[:space:]])(sorry|admit|native_decide)([[:space:]]|$)|^[[:space:]]*axiom[[:space:]]'
          if grep -R -n -E "$pattern" --include='*.lean' Sqrt6KissingBound Sqrt6KissingBound.lean; then
            exit 1
          fi
          mkdir -p .ci
          printf 'analytic, cone, disjoint-cap, finite-packing, and cancellation infrastructure verified at %s\n' "$(git rev-parse HEAD)" > .ci/current-infrastructure.ok
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add .ci/current-infrastructure.ok
          git commit -m 'Record verified analytic and cap-packing infrastructure' || true
          git push origin HEAD:gpt-pro/full-lean-certificate
